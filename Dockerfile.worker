FROM node:24-alpine

WORKDIR /app

RUN corepack enable

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile --prod --ignore-scripts

COPY tsconfig.json ./
COPY src ./src

ENTRYPOINT ["pnpm", "exec", "tsx", "src/worker/main.ts"]
